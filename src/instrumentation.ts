/**
 * This file is intentionally empty.
 * The instrumentation hook lives at the project root: instrumentation.ts
 * Next.js requires the file at the root (or src/) — only one register()
 * export should exist to avoid double-registration at startup.
 */
